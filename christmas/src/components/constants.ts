export enum Colors {
  white = '#fff',
  main = '#278D9F',
}

export enum Coefficients {
  percent = 100,
}

export enum Phrases {
  noMatch = 'Извините, совпадений не обнаружено',
}

export enum CardParameters {
  width = 190,
  height = 280,
  gap = 30,
}